version: '3.8'

services:
  # QuitCode API
  api:
    build:
      context: .
      dockerfile: deploy/Dockerfile
    ports:
      - "8001:8001"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - REDIS_HOST=redis
      - QDRANT_HOST=qdrant
      - OPENCODE_DEBUG=false
      - OPENCODE_LOG_LEVEL=INFO
    depends_on:
      - redis
      - qdrant
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
    networks:
      - quitcode-network

  # Redis
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes
    restart: unless-stopped
    networks:
      - quitcode-network

  # Qdrant Vector DB
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant-data:/qdrant/storage
    environment:
      - QDRANT__SERVICE__GRPC_PORT=6334
    restart: unless-stopped
    networks:
      - quitcode-network

  # PostgreSQL (for skill memory and audit)
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=quitcode
      - POSTGRES_PASSWORD=quitcode
      - POSTGRES_DB=quitcode
    volumes:
      - postgres-data:/var/lib/postgresql/data
    restart: unless-stopped
    networks:
      - quitcode-network

volumes:
  redis-data:
  qdrant-data:
  postgres-data:

networks:
  quitcode-network:
    driver: bridge
